to create the tables for database XEPDB1 (the database of this app)

run the command `@C:\path\to\database.sql` inside the SQL plus shell 

Then change the Creds of ur ORACL database inside the ile connexion.php
